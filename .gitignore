__pycache__/
*.pyc

# all cached/generated artifacts - SAE checkpoints, masks, activations,
# log-odds, adapters. These are regenerated by running the pipeline,
# so they don't belong in git.
sae_pruning_cache/

# common local-env clutter
.ipynb_checkpoints/
.venv/
venv/
.DS_Store
